[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Deja que el Dockerfile defina CMD, o usa uno solo de estos dos (no ambos).
# startCommand = "node server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
numReplicas = 1
# Production: always-on SaaS (no sleep)
sleepApplication = false

# Staging: sleep when idle to burn less of the Pro $20 credit while idle
[environments.staging.deploy]
sleepApplication = true
numReplicas = 1
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[variables]
# Público/operativo (OK en repo)
TZ = "America/Tegucigalpa"
NODE_ENV = "production"
PORT = "8080"
HOSTNAME = "0.0.0.0"
NEXT_TELEMETRY_DISABLED = "1"
DEFAULT_CURRENCY = "HNL"
DEFAULT_TIMEZONE = "America/Tegucigalpa"
SKIP_ENV_VALIDATION = "false"

# Estas NO van en el repo. Configúralas en Railway Dashboard:
# NEXT_PUBLIC_SUPABASE_URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY
# SUPABASE_SERVICE_ROLE_KEY
# DATABASE_URL
# SUPABASE_JWT_SECRET
# SESSION_SECRET
# REDIS_URL / REDIS_PRIVATE_URL (prefer private reference from Redis service)
# CRON_SECRET

[railway]
serviceType = "web"